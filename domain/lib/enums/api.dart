// ignore_for_file: constant_identifier_names

enum ApiStatus {
  OK,
  SUCCESS,
  FAILURE,
  EXCEPTION,
  INVALID_VALUES,
}

enum AppURLsType { main, }

enum HttpMethod {
  get,
  post,
  put,
  patch,
  delete,
}

enum DataResponseType { json, stream, plain, bytes }